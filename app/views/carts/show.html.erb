<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <%= render 'empty_cart_alert' if cart_subtotal_cents == 0 %>
  <%= render 'cart_with_items' if cart_subtotal_cents > 0 %>
  
</section>
